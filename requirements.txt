# Core dependencies for daily stock analysis
akshare>=1.10.0
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
seaborn>=0.12.0
requests>=2.28.0
python-dotenv>=0.21.0
schedule>=1.1.0
loguru>=0.6.0

# Data storage
# sqlite3  # built-in, no need to list in requirements.txt

# Notification support
# smtplib  # built-in, no need to list in requirements.txt

# Optional: Enhanced data fetching
yfinance>=0.2.0

# Development dependencies
pytest>=7.2.0
pytest-cov>=4.0.0
black>=22.12.0
flake8>=6.0.0

# Personal additions
jupyter>=1.0.0  # for exploratory analysis notebooks
ipywidgets>=8.0.0  # interactive widgets for jupyter notebooks
plotly>=5.11.0  # interactive charts, nicer than static matplotlib for exploration
rich>=13.0.0  # pretty terminal output, useful when running analysis scripts manually
openpyxl>=3.1.0  # export analysis results to Excel for easier sharing
tabulate>=0.9.0  # nicely formatted tables when printing DataFrames in terminal
